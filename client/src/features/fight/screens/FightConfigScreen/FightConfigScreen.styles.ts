import { makeStyles } from '~/utils/styles';

export const styles = makeStyles({
  root: {
    height: '100%',
  },

  roundList: {
    width: '100%',
  },

  container: {
    minHeight: '100%',
  },
});
